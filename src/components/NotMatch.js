import React,{PureComponent} from 'react'

export default class NotMatch extends PureComponent {
     render(){
          return(
              <div> 
                <h1>Error Page</h1>
              </div>
          )
     }
}